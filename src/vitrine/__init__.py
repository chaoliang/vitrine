# -*- coding: utf-8 -*-
"""vitrine —— 配置驱动的服饰商品视频产线。

One config file produces one vertical product video. Changing the product means
changing the config, never the code; changing the renderer means adding a
backend, never touching the merchandising rules.
"""
__version__ = "0.1.0"
